### Rust / Cargo
/target/
**/*.rs.bk
# Cargo.lock is committed (binary crate) for reproducible CI/release builds.

### IDEs / Editors
.vscode/

### OS junk
.DS_Store
Thumbs.db

### Project-specific
dsc.toml
test-dsc.toml
testdsc.toml
demo-dsc.toml

### Generated shell completions (regenerate with `dsc completions <shell>`)
/completions/

### Private notes
.private/

### Command output that can carry personal data or forum contents
# SAR bundles (`dsc sar`) hold subject-access data including private messages;
# backup archives (`dsc backup pull`) hold the whole forum database.
/sar-*/
*.tar
*.tar.gz

### Reference PDFs / third-party copyrighted material (keep out of the repo)
*.pdf

### Zensical docs build output
/site/

### Local browser automation captures
/.playwright-mcp/
